# WeRSS（we-mp-rss）本地使用说明

> 适用环境：Windows 11 24H2 64 位，本机已禁用 Docker、禁止本地编译，所有依赖由 `uv` 管理。
> 软件用途：把微信公众号文章转成 RSS 订阅源，并提供后台管理界面。
> 本文档从「重新开机」开始，一步步带你启动程序、登录后台、授权微信账号，直到可以订阅公众号。

---

## 0. 一句话快速开始（熟练后看这节）

```bat
:: 以管理员或普通用户打开 一个 终端（CMD / PowerShell / Git Bash 均可），依次执行：
cd /d D:\Documents\AI_Work_Temp\we-mp-rss
set USERNAME=admin
set PASSWORD=admin@123
set CHROME_EXECUTABLE_PATH=D:\Tools\360Chrome\360chromex.exe
uv run --project . python main.py -job True -init True
```

然后浏览器打开 `http://127.0.0.1:8001/` → 用 `admin / admin@123` 登录 → 左侧「授权管理」点「获取授权」→ 弹出 360Chrome 窗口扫码登录微信 → 完成。

---

## 1. 环境已经就绪（无需再安装）

本机已完成一次性安装与配置，你**不需要**重复安装。各关键路径如下：

| 项目 | 路径 / 值 | 说明 |
|---|---|---|
| 项目目录 | `D:\Documents\AI_Work_Temp\we-mp-rss` | 所有命令都在此目录下执行 |
| Python 运行时 | `uv` 管理的虚拟环境 `.venv`（Python 3.13） | 禁止直接用裸 `python`/`pip` |
| `uv` 可执行文件 | `D:\Tools\Assembly\uv\uv.exe`（已在 PATH） | 命令直接用 `uv` 即可 |
| 浏览器（微信登录用） | `D:\Tools\360Chrome\360chromex.exe` | 360 极速浏览器，用于驱动微信扫码 |
| 服务端口 | `8001` | 后台网页地址 `http://127.0.0.1:8001/` |
| 数据目录 | `D:\Documents\AI_Work_Temp\we-mp-rss\data` | 数据库、日志、锁文件都在这 |
| 管理员账号 | `admin` / `admin@123` | 后台网页登录凭据 |

> 说明：本机没有安装 Chrome / Edge，微信授权专门为 360Chrome 做了适配（可见窗口 + 关闭反爬注入 + 自动切到扫码模式），请勿改动 `driver/playwright_driver.py` 与 `driver/wx.py` 中的相关开关，否则会重新出现"取不到二维码"。

---

## 2. 重新开机后：启动程序

每次重启电脑后，服务不会自动运行，需要手动启动一次。

### 2.1 打开终端

任选一种方式打开命令行窗口（**保持这个窗口一直开着**，它就是服务器，关掉服务就停了）：

- **CMD**：按 `Win + R`，输入 `cmd`，回车。
- **PowerShell**：右键开始菜单 →「终端」或「Windows PowerShell」。
- **Git Bash**：如果你装了 Git，右键文件夹选「Git Bash Here」。

### 2.2 进入项目目录并启动

**CMD / Git Bash：**
```bat
cd /d D:\Documents\AI_Work_Temp\we-mp-rss
set USERNAME=admin
set PASSWORD=admin@123
set CHROME_EXECUTABLE_PATH=D:\Tools\360Chrome\360chromex.exe
uv run --project . python main.py -job True -init True
```

**PowerShell（语法不同）：**
```powershell
cd D:\Documents\AI_Work_Temp\we-mp-rss
$env:USERNAME="admin"; $env:PASSWORD="admin@123"; $env:CHROME_EXECUTABLE_PATH="D:\Tools\360Chrome\360chromex.exe"
uv run --project . python main.py -job True -init True
```

### 2.3 确认启动成功

- 启动后终端会刷出一堆日志，**约 10–15 秒**后服务就绪。
- 看到类似 `Uvicorn running on http://0.0.0.0:8001` 或不再报错即表示成功。
- 验证：另开一个浏览器访问 `http://127.0.0.1:8001/`，应看到登录页「微信公众号订阅助手」。

> 为什么每次要带 `set USERNAME=admin`？
> 程序初始化账号时默认取本机 Windows 登录名（本机是 `zhangweildlh`），会导致管理员账号名不是 `admin`。
> 设置这两个环境变量后，初始化会直接维护 `admin` 账号，避免登录时用户名对不上。即使你已经改好过数据库，加上这两个变量也最稳妥。

> 想后台静默运行（不占终端）？可在命令后加 ` &`（Git Bash）/ `start`（CMD）/ 用 `Start-Process`（PowerShell），但新手建议先保持前台，方便看日志。

---

## 3. 登录网页后台（输入账号密码）

1. 浏览器打开 `http://127.0.0.1:8001/`（推荐 Chrome / Edge / 360 浏览器）。
2. 在登录页输入：
   - **用户名**：`admin`
   - **密码**：`admin@123`
3. 点击「登录」。
4. 登录成功后进入后台首页，左侧导航有「订阅管理」「授权管理」等菜单。

> 如果提示"用户名或密码错误"：
> - 先确认服务是用上面带 `USERNAME=admin PASSWORD=admin@123` 的命令启动的；
> - 若仍不行，多半是数据库里管理员账号名被初始化成了 `zhangweildlh`，需要把 `users` 表的用户名改回 `admin`（详见第 7 节排查）。

---

## 4. 授权微信账号（在浏览器里登录微信）

这是「登录浏览器、输入微信账号密码」的环节。目标是让 WeRSS 拿到你公众号后台的登录态，才能去抓取文章。

### 4.1 触发授权

1. 在后台左侧点「**授权管理**」。
2. 页面上点「**获取授权**」（或类似"扫码登录"按钮）。
3. 程序会**自动弹出 360Chrome 浏览器窗口**（这是真实的、你能看见的窗口，不是后台隐藏窗口）。

### 4.2 方式 A：手机微信扫码登录（推荐，默认）

- 弹出的 360Chrome 会自动打开 `https://mp.weixin.qq.com` 并切到「**扫码登录**」标签页，显示一张二维码。
- 同时，网页后台的「授权管理」区域大约 **15 秒后**也会显示同一张二维码图片（"正在获取二维码..."会消失）。
- 打开手机上的**微信 App** → 点右上角「+」→「扫一扫」→ 扫描二维码。
- 手机上点「登录」确认。
- 登录成功后：360Chrome 窗口会自动跳转到公众号首页并**自动关闭**，后台提示「授权成功」。

### 4.3 方式 B：在浏览器里输入微信账号密码登录（备选）

如果你更习惯输密码，也可以不用扫码：

1. 在弹出的 360Chrome 窗口里，点「**使用账号登录**」标签（位于扫码登录旁边）。
2. 输入你的微信绑定账号：
   - 微信号 / 绑定的 QQ 号 / 绑定的邮箱，任选其一；
   - 再输入**微信密码**。
3. 若微信要求滑块验证、短信/好友辅助验证，按页面提示完成。
4. 登录成功后窗口同样会跳到公众号首页并自动关闭，后台显示授权成功。

> 注意：无论用扫码还是输密码，只要最终登录进 `mp.weixin.qq.com` 的公众号首页，程序都会自动识别并保存登录态。两种方式等价。

---

## 5. 开始使用：添加公众号并生成 RSS

授权成功后，就可以订阅你想追的公众号了。

1. 左侧点「**订阅管理**」（页面里也叫「公众号管理」）。
2. 点「**添加公众号**」按钮。
3. 按提示填入要订阅的公众号信息（通常是公众号名称或原始 ID），保存。
4. 添加后，程序会按设定间隔自动抓取该号的文章。
5. 在文章列表里选中某个公众号，可点「**RSS**」生成该号的 RSS 订阅链接，复制到其他 RSS 阅读器使用。
6. 还可在「消息任务」「标签」等菜单里配置推送到钉钉 / 企业微信 / 邮箱等（按需，非必须）。

> 至此你已经完成「开机 → 启动 → 登录 → 授权微信 → 添加公众号」的完整起步流程。

---

## 6. 停止与重启

- **正常停止**：回到启动服务的那个终端窗口，按 `Ctrl + C`，等它退出即可。
- **重启**：直接再执行第 2.2 节的启动命令（若 8001 端口还被旧进程占着，先按下面排查杀掉）。
- **查看是否在运行**：浏览器访问 `http://127.0.0.1:8001/`，能打开就说明在跑。

---

## 7. 常见问题排查

### 7.1 卡在"正在获取二维码..."

页面一直显示"正在获取二维码..."，二维码图片不出。按以下顺序处理：

1. **确认服务是最新代码启动的**：旧的后端进程可能还占着 8001 端口。重启服务（先 `Ctrl+C` 停掉，再执行第 2.2 节命令）。
2. **清残留锁/旧图片**（最新代码已能自动恢复，但手动清一次更稳）：
   ```bat
   cd /d D:\Documents\AI_Work_Temp\we-mp-rss
   del static\wx_qrcode.png 2>nul
   del data\lock.lock 2>nul
   ```
   然后刷新后台页面、重新点「获取授权」。
3. **确认 360Chrome 能单独打开微信后台**：手动双击 `D:\Tools\360Chrome\360chromex.exe`，访问 `https://mp.weixin.qq.com` 能正常显示，说明浏览器本身没问题。
4. 若仍不行，看 `data\server.log` 末尾的报错，把现象反馈处理。

### 7.2 扫码后后台仍显示"未登录"

- 最常见原因：登录进的是**个人微信**，而不是**微信公众平台**（mp.weixin.qq.com）。必须用公众号账号登录。
- 扫码后请确认 360Chrome 窗口已经跳到 `mp.weixin.qq.com/cgi-bin/home` 公众号首页；没跳到就不算成功。
- 若窗口跳到了首页但后台没更新，重启一次服务再授权即可。

### 7.3 提示"微信公众平台登录脚本正在运行"

说明上一次授权没正常结束、残留了锁文件。执行 7.1 第 2 步清理，或直接重启服务（最新代码会在检测到残留锁时自动清理并重启）。

### 7.4 后台登录提示"用户名或密码错误"

- 确认启动命令带了 `set USERNAME=admin` 与 `set PASSWORD=admin@123`。
- 若数据库被初始化成了别的用户名（如 `zhangweildlh`），用任意 SQLite 工具执行：
  ```sql
  UPDATE users SET username='admin' WHERE username='zhangweildlh';
  ```
  再用 `admin / admin@123` 登录。

### 7.5 端口 8001 被占用、启动报地址已在使用

1. 找出占用端口的进程并结束它：
   - **PowerShell：**
     ```powershell
     $p = (Get-NetTCPConnection -LocalPort 8001 -State Listen).OwningProcess
     Stop-Process -Id $p -Force
     ```
   - **CMD（用资源监视器或 `netstat` 找到 PID 后）：**
     ```bat
     taskkill /PID <占用进程的PID> /F
     ```
     > 注意：`taskkill` 用**单斜杠** `/PID`，不要写成 `//PID`。
2. 再重新执行启动命令。

---

## 8. 重要配置与约定（改之前先读）

- **不要改** `driver/playwright_driver.py`、`driver/wx.py` 里与微信登录相关的开关：
  - `PlaywrightController(headless=False, apply_anti_crawler=False)` —— 必须可见、且关闭反爬注入，否则微信登录页 JS 被 CORS 拦截、取不到二维码。
  - `wxLogin` 里"自动点击扫码登录标签"逻辑 —— 微信默认停在账号密码模式，需切到扫码模式二维码才可见。
- **360Chrome 路径已改为"环境变量驱动、不写死"**：浏览器可执行文件由 `driver/chrome_path.py` 按 `CHROME_EXECUTABLE_PATH` 环境变量（或本机标准安装位置）解析，**代码里不再写死任何绝对路径**。不要改回写死路径，否则会破坏跨平台（Win11 / Docker-Linux）通用性；要换浏览器只改 `CHROME_EXECUTABLE_PATH` 这个环境变量即可。
- **环境变量**（可在 `config.yaml` 同级或系统里设置）：
  - `CHROME_EXECUTABLE_PATH`：本地 Chromium 内核浏览器路径（如 `D:\Tools\360Chrome\360chromex.exe`）。不设置时自动回退到 Playwright 自带浏览器；Docker/Linux 默认不设即走自带浏览器，行为不变。
  - `WERSS_AUTH_WEB`：默认 `False`，走浏览器扫码路线；保持 `False`。
  - `PORT`：服务端口，默认 `8001`。
  - `USERNAME` / `PASSWORD`：初始化管理员账号用，建议固定为 `admin` / `admin@123`。
- **PDF 导出**已开启（`config.yaml` 中 `export.pdf.enable: true`），在文章详情页可使用导出 PDF。
- **数据备份**：`data/` 目录（含 `db.db` 数据库）是整个程序的状态，重装系统或迁移前请备份此目录。

---

## 9. 命令速查表

| 目的 | 命令（CMD / Git Bash） |
|---|---|
| 启动服务 | `cd /d D:\Documents\AI_Work_Temp\we-mp-rss && set USERNAME=admin && set PASSWORD=admin@123 && set CHROME_EXECUTABLE_PATH=D:\Tools\360Chrome\360chromex.exe && uv run --project . python main.py -job True -init True` |
| 浏览器访问 | 打开 `http://127.0.0.1:8001/` |
| 清理授权残留 | `del static\wx_qrcode.png && del data\lock.lock` |
| 停止服务 | 启动终端里按 `Ctrl + C` |
| 释放 8001 端口 | `taskkill /PID <PID> /F` |

---

*本文档对应已修复微信扫码登录的最终版本（微信授权走可见 360Chrome + 关闭反爬注入 + 自动切扫码模式 + 残留锁自动恢复）。如后续代码有变动，以实际界面与 `data/server.log` 为准。*
